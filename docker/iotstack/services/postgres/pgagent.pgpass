# pgAgent .pgpass for all DBs
# the file must be set to permission 600 on the host
# chmod 600 ./services/postgres/pgagent.pgpass
# SECRET REDACTED 2026-08-15 (capture into this repo) - real password lives on
# the host at /mnt/db/IOTstack/services/postgres/pgagent.pgpass (mode 600).

postgres:5432:*:pgagent:<REDACTED>
